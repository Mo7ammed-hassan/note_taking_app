class NoteCardItemModel {
  final String title, image;
  final double size;
  final int files;

  const NoteCardItemModel({
    required this.title,
    required this.image,
    required this.size,
    required this.files,
  });
}
